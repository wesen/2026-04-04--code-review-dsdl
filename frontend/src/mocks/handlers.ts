/**
 * App-level MSW request handlers.
 * Re-exports all handlers from the cr-walkthrough package.
 * Add app-specific handlers below when needed.
 */
export { handlers as packageHandlers } from '@crs-cradle/cr-walkthrough/mocks';
